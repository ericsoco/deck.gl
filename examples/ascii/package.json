{
  "scripts": {
    "start-local": "webpack-dev-server --env.local --progress --hot --open",
    "start": "webpack-dev-server --progress --hot --open",
    "build": "webpack -p --output-filename bundle.js"
  },
  "dependencies": {
    "deck.gl": "^5.1.0",
    "deck.gl-layers": "^0.0.23",
    "luma.gl": "^5.1.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  },
  "devDependencies": {
    "buble-loader": "^0.4.0",
    "webpack": "^2.2.0",
    "webpack-dev-server": "^2.2.0"
  }
}
